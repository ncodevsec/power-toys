{
	"name": "power-toys",
	"version": "0.5.2",
	"description": "Essential toolkit for pentesters and bug hunters. Quickly extract links, encode/decode data, and simplify everyday security tasks.",
	"main": "build.js",
	"directories": {
		"doc": "docs"
	},
	"scripts": {
		"build": "node build.js",
		"build:css": "tailwindcss -i ./src/styles/input.css -o ./src/styles/tailwind.min.css",
		"build:all": "concurrently \"npm run build\" \"npm run build:css\"",
		"watch": "node build.js --watch",
		"watch:css": "tailwindcss -i ./src/styles/input.css -o ./src/styles/tailwind.min.css --watch",
		"watch:all": "concurrently \"npm run watch\" \"npm run watch:css\""
	},
	"keywords": [
		"security",
		"pentesting",
		"bug-hunting",
		"encoding",
		"decoding",
		"chrome-extension",
		"firefox-extension"
	],
	"author": "ncodevsec",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/ncodevsec/power-toys"
	},
	"homepage": "https://github.com/ncodevsec/power-toys",
	"dependencies": {
		"@tailwindcss/cli": "^4.3.0",
		"tailwindcss": "^4.3.0"
	},
	"devDependencies": {
		"concurrently": "^8.2.0"
	}
}
